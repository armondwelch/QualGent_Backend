import { Client as WebDriverClient } from "webdriver";
import { IDevice } from "../types";
export interface AppwrightVision {
    extractText(prompt: string): Promise<string>;
    tap(prompt: string): Promise<void>;
}
export declare class VisionProvider {
    private device;
    private webDriverClient;
    constructor(device: IDevice, webDriverClient: WebDriverClient);
    extractText(prompt: string): Promise<string>;
    tap(prompt: string): Promise<void>;
}
//# sourceMappingURL=index.d.ts.map