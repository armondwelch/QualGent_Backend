import { Client as WebDriverClient } from "webdriver";
import { ElementReference, TestInfoOptions, WaitUntilOptions } from "../types";
export declare class Locator {
    private webDriverClient;
    private selector;
    private findStrategy;
    private testOptions;
    private textToMatch?;
    constructor(webDriverClient: WebDriverClient, selector: string | RegExp, findStrategy: string, testOptions: TestInfoOptions, textToMatch?: string | undefined);
    fill(value: string, options?: WaitUntilOptions): Promise<void>;
    sendKeyStrokes(value: string, options?: WaitUntilOptions): Promise<void>;
    isVisible(options?: WaitUntilOptions): Promise<boolean>;
    private waitUntil;
    tap(options?: WaitUntilOptions): Promise<void>;
    getText(options?: WaitUntilOptions): Promise<string>;
    getElement(): Promise<ElementReference | null>;
}
//# sourceMappingURL=index.d.ts.map