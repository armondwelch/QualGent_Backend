import { AppwrightConfig, DeviceProvider } from "../../types";
import { FullProject } from "@playwright/test";
import { Device } from "../../device";
export declare class BrowserStackDeviceProvider implements DeviceProvider {
    private sessionDetails?;
    private sessionId?;
    private project;
    constructor(project: FullProject<AppwrightConfig>);
    globalSetup(): Promise<void>;
    getDevice(): Promise<Device>;
    private createDriver;
    private getSessionDetails;
    private getAppBundleId;
    downloadVideo(outputDir: string, fileName: string): Promise<{
        path: string;
        contentType: string;
    } | null>;
    syncTestDetails(details: {
        status?: string;
        reason?: string;
        name?: string;
    }): Promise<any>;
    private createConfig;
}
//# sourceMappingURL=index.d.ts.map